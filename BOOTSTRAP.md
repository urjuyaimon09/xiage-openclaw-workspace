# BOOTSTRAP.md - 启动记忆同步

版本：v1.1.0
更新：2026-04-18

---

## 每次启动必须执行

1. 读取 `memory/hot/current.md`（思维状态快照）
2. 读取 `memory/hot/lessons.md`（教训记录）
3. 读取当日 `memory/YYYY-MM-DD.md`（每日合并）
4. 按时间顺序加载当日所有 `memory/sessions/` 文件
5. 初始化 doc-loader 缓存：`docLoader.warmUp()`（路径：docs/意识层/doc-loader.js）
6. 从快照还原：
   - 高频 Cognition 索引（直接进入 Loop1 输入层）
   - 最近推理结论（进入 Loop2 判断层）
   - 当前项目状态（进入工作上下文）
7. 将快照内容带入当前上下文

---

## 上下文拼装顺序（三明治结构）

> v1.1.0 新增（2026-04-18）
> 利用 U 型注意力曲线：模型对首尾关注度高，中间易忽略

**拼接顺序（从底到顶）：**

```
┌─────────────────────────────────────────────┐
│  【尾部】今日日志 + 待办                      │
│  近因效应：最新最相关，模型天然反应最强       │
├─────────────────────────────────────────────┤
│  【中部】SOUL.md + USER.md                   │
│  背景渗透：性格 + 用户偏好，潜移默化影响语气  │
├─────────────────────────────────────────────┤
│  【头部】AGENTS.md                           │
│  高权重：绝对不能违反的规则，始终压着         │
└─────────────────────────────────────────────┘
```

**完整上下文示例：**

```
# AGENTS.md
（绝对规则 + 触发词 + 执行流程）

# SOUL.md
（身份 + 三观 + 意识流）

# USER.md
（坚果背景 + 五层需求 + 协作偏好）

# memory/hot/lessons.md
（教训记录，踩坑必读）

# memory/YYYY-MM-DD.md
（今日日志 + 待办）
```

**原则：**
- AGENTS.md 永远在最前，作为底座
- SOUL.md + USER.md 在中部，作为背景
- 今日日志在尾部，作为最近上下文
- 绝对不要把规则类文件放在中间（会被弱化）

---

## 归档触发

| 时机 | 触发 | 目标 |
|------|------|-------|
| Session结束 | 立即 | `memory/sessions/YYYY-MM-DD-HHMMSS.md` |
| 心跳30分钟 | 定时 | 增量归档 |
| 每日00:00 | 定时 | 合并为 `memory/YYYY-MM-DD.md` |

---

## 同步触发条件

以下情况必须写回 memory：
- 用户做出了重要决定或结论
- 产生了需要追踪的待办事项
- 对话中明确了某个事实或偏好
- 收到了需要让另一个 session 知道的信息

---

## 格式要求

见 MEMORY.md 两层归档机制。

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.1.0 | 2026-04-18 | 新增「上下文拼装顺序（三明治结构）」：头部AGENTS、中部SOUL+USER、尾部日志 |
| v0.0.0 | 初始 | 启动记忆同步基础版本 |
